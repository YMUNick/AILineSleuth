# Roadmap：LineSleuth（2026-09-24 → 10/18）

- 建立：2026-09-24，Paula（PM）
- 更新：2026-09-24（第二次會議後）：新增「本週重點」一節（老闆日期清單、GCP 開通當天路徑、真 Gemini 通過標準、凍結清單、Cloud Run 常駐政策、本機憑證）；真 Gemini＋最簡 Cloud Run 從 W2/W3 提前到 GCP 開通當天
- 更新：2026-09-24（第三次會議後）：新增「部署後定案」一節（已宣告完成項、BigQuery 已砍、10/3／10/8／10/10／10/17 日期、老闆待辦）；Cloud Run 常駐政策改成 10/17 錄影前調 1、繳交後回 0
- 依據：`docs/meetings/2026-09-24-駭客松主題方向.md`、`docs/meetings/2026-09-24-下一步與優化.md`、`docs/meetings/2026-09-24-部署後下一步.md`、`docs/prd.md`
- 前提：程式只有老闆一個人寫，總工時估 60–80 小時，每週大約需要 25 小時。表格中的「負責」是指哪個角色產出文件或規格；實際動手寫程式、部署的都是老闆。

## 部署後定案（2026-09-24 第三次會議）

**已宣告完成，不再動**：
- Cloud Run 部署（`asia-southeast1`、`gemini-2.5-flash`、映像內建 DuckDB、min 0／max 1）
- 真 Gemini 完整回歸 GATE PASS（10/10 根因、2/2 證據不足、3 次一致、中位數 12.6 秒，見 `docs/qa/runs/README.md`）
- 手機實機掃 QR 開工單

**砍掉／延後**：
- **BigQuery 已砍**：資料一律寫「示範用 DuckDB，可換 BigQuery」，不寫「已支援」。Google Cloud 使用深度靠 Cloud Run＋Vertex AI Gemini。
- X-Forwarded-For 偽造檢查延到繳交之後（前提：每日上限先上線）。
- 3.8-flash 只當備案，錄影當天實際切換一次。

**定案日期**：

| 日期 | 事項 | 負責 | 完成定義 |
|---|---|---|---|
| 10/1（四） | 停損點 ①：回報隊友、訪談、預算狀態；查試用抵免額（約 US$381）到期日 | 老闆 | 見下方「10/1 停損點 ①」；仍 0 場訪談時 pitch 拿掉 40 分鐘、只講「實測約 12 秒」 |
| 10/1 前 | GCP 端設 Vertex AI API 每日配額上限（IAM 與管理 → 配額），當公開網址被刷時的硬上限 | 老闆 | 配額已設；數值待估算 |
| 10/3（六） | `docs/pitch/storyboard-30s-image-prompts.md` 改版（只留 01 生成圖 prompt） | Dana | v0.2 已交 |
| 10/10 前 | A1（720 高第一屏至少 3 列證據）、每日調查上限 200 次（超過顯示友善畫面）、BigQuery 描述改寫 | Eddie | Quinn 10/8 驗證通過 |
| 10/8（四） | 雲端驗證五項：① 完整回歸集全過 ② min-instances=1 冷啟動 < 3 秒 ③ 真 Gemini 下 prompt injection 無外洩 ④ 超過每日上限顯示友善畫面 ⑤ A1 在 720 高不破版 | Quinn | 五項全 PASS，寫進 `docs/qa/runs/README.md` |
| 10/10（六） | **影片與 PPT 凍結**（30 秒 C 版與繳交版 A 版） | Dana、Sandy | 之後只修錯字與破圖 |
| 10/17（六） | min-instances 調成 1 後錄影；錄前確認 min=1、先打一次暖機、網路走手機熱點、3.8-flash 實際切換一次 | 老闆／Quinn | 錄影完成 |
| 繳交後 | min-instances 調回 0；max-instances 維持 1 | 老闆 | Cloud Run 設定已改回 |

**每日上限兩層都要**：程式內上限 200 次負責友善畫面與正常情況（min 0 時實例關閉會歸零）；GCP 端 API 配額負責硬上限。

## 本週重點（2026-09-24 第二次會議決議）

**現況**：UI v2 已上線，但 196 條測試全是離線假資料，「AI 真的準」和參賽資格（隊友、GCP、訪談）都還是零證據。

**優先順序**：① 老闆本人的資格項（隊友、GCP、訪談）→ ② 真 Gemini＋最簡 Cloud Run → ③ BUG-009 → ④ A1 版面。

### 老闆本週日期清單

| 日期 | 事項 | 完成定義 | 協助 |
|---|---|---|---|
| 9/25（五） | 發第一批 10 則訪談邀約（LinkedIn 搜廠長／生管課長、台商協會），之後每天 10 則，3 天沒回就發跟進版；同一天在 GDG Taiwan 和比賽 Discord 貼徵隊友文 | 邀約和徵人文都已發出 | Sandy（`docs/sales/outreach-message.md` 短版、徵隊友話術） |
| 9/26（六） | 拍板 GCP 預算上限；開通 GCP；設預算警示 50／90／100%＋「預測達 100%」；本機登入 ADC（見下方「本機憑證」） | 帳單頁看得到四條警示 | Felix（上限提案）、Eddie |
| 9/27（日） | 和 Eddie 一起打第一次真 Gemini：R01、N01 各 10 次 | 符合下方「第一天」通過標準，log 有 token 數 | Eddie、Quinn（判定）、Felix（回填 `roi-model.csv` 第 15 列） |
| 9/28（一）前 | 約好 2 場訪談 | 2 場的日期時間都確定 | Sandy |
| 10/1（四） | 停損點 ①，**標準不放寬** | 見下方「10/1 停損點 ①」 | — |

GCP 沒在 9/26 開通，9/27 的真 Gemini 就跟著順延；但隊友和 GCP 仍以 10/1 為死線。

**工程端（Eddie，GCP 開通前，9/26 前交）**：BUG-009（4 條 strict xfail 轉成 passed）、ENH-002 token 紀錄、Gemini／BigQuery 假 client 測試（斷網全過）、Gemini 逾時與 429 重試上限、A1＋小圖（Dana 看截圖驗收）、A9 最後做。驗收清單由 Quinn 寫在 `docs/qa/test-plan.md`。

### GCP 開通當天路徑

目標：半天內有可以公開開啟的網址。

1. 設預算警示（50／90／100%＋預測達 100%）
2. 開 API（清單見 `docs/engineering/deploy.md`）
3. 本機用 ADC 登入，跑 `tests/test_gemini_live.py` 打第一次真 Gemini
4. R01、N01 各跑 10 次，確認 log 有 token 數；照下方「第一天」標準判定，沒過就停在這一步
5. Cloud Run 先用映像檔內建的 DuckDB 上線（min-instances=0），並計時冷啟動第一次請求
6. ~~隔天切換到 BigQuery~~ → 9/24 第三次會議砍掉：資料維持**示範用 DuckDB，可換 BigQuery**，不切換、對外不寫「已支援」
7. 全部回歸集 `--repeat 3`
8. 手機實機掃工單 QR

### 真 Gemini 通過標準（Quinn）

| 階段 | 標準 |
|---|---|
| 第一天（R01／N01 各 10 次） | R01 必須 10/10；N01 灰卡雙向全過；任何一次出現「錯根因＋High」直接 FAIL |
| 全部回歸集（`--repeat 3`） | ≥ 9/10；同一題 3 次結論一致 |
| 沒通過時 | 先分類是哪一種錯，再改 prompt；**不放寬門檻**。改用 2.5-flash（preview 模型改名或下架時的備案）要整套重跑 |

token 成本只用第一天 R01／N01 各 10 次的紀錄來算，不另外多跑：Felix 取中位數乘官方單價，回填 `docs/finance/roi-model.csv` 第 15 列；10/1 依帳單回填第 16 列（GCP 月費）。

### Cloud Run 常駐政策

- **10/17 錄影前一律 min-instances=0**，避免 24 小時常駐計費；代價是冷啟動，第一次請求要實測秒數。
- ~~10/16–10/18 和評審期改成 1~~ → 9/24 第三次會議定案：**10/17 錄影前調成 1，繳交後調回 0**；max-instances 維持 1（每個實例各算各的上限）。常駐期間的成本待估算（Eddie 用 GCP 計價工具算）。

### 本機憑證

- 本機用 `gcloud auth application-default login`（ADC）；Cloud Run 用服務帳號身分（`docs/engineering/deploy.md` 第 2 節）。
- **不下載服務帳號 JSON 金鑰**。`.env` 只放專案 ID、區域這類非機密設定；專案 ID、帳單資訊也不要貼在聊天裡。

### 凍結清單（第三次會議定案：影片與 PPT 於 10/10 凍結）

以下項目這段期間不改：

- PPT（`docs/pitch/LineSleuth-demo.pptx`）
- Demo 影片（`docs/pitch/demo-video-storyboard.md`）
- pitch 裡的所有數字
- 影片分鏡 S05／S06 對調
- 提前錄影

例外：Sandy 可以先替每一句含數字的 pitch 準備「無數字版」。10/8 仍然 0 場訪談時改用無數字版，引用公開產業資料並註明「訪談進行中」，不編造客戶原話。

## 逐週里程碑

### W1：9/24（四）– 9/30（三）— 資格、GCP、訪談、分鏡

| 項目 | 負責 | 完成定義 |
|---|---|---|
| PRD、Roadmap | Paula | `docs/prd.md`、`docs/roadmap.md` 完成 |
| 開通 GCP 專案，設預算警示（50/90/100%＋預測達 100%） | Eddie（規格）／老闆（執行） | 在帳單頁看得到警示設定；確認贈金與免費額度；9/26 |
| 真 Gemini 第一次呼叫（R01／N01 各 10 次）＋最簡 Cloud Run（DuckDB） | Eddie、Quinn／老闆（執行） | 從 W2/W3 提前；照「GCP 開通當天路徑」與「真 Gemini 通過標準」 |
| 開通前工程收尾：BUG-009、ENH-002、假 client 測試、重試上限、A1＋小圖、A9 | Eddie | 9/26 前；Quinn 驗收清單全過 |
| GCP 現金上限提案 | Felix | 交給老闆拍板（PRD Q3） |
| 約 3 位廠長訪談並完成 2–3 場 | Sandy（題綱、邀約文）／老闆（實際訪談） | 使用 `docs/sales/interview-guide.md`，當天填好紀錄表 |
| 訪談數字表 | Felix | `docs/finance/interview-metrics.md`，並回填 `roi-model.csv` |
| 寫信向主辦確認隊友資格和「可運作原型」的定義 | Quinn（擬稿）／老闆（寄出） | 信件已寄出 |
| 分鏡稿、Figma 三頁、灰卡規格 | Dana | `docs/design/storyboard.md`（期限 10/1） |
| 模擬資料 schema 和 5 個固定查詢函式的設計 | Eddie | 依照 PRD 第 8 節的故事設計 |

### 10/1（四）停損點 ①

必須**全部**做到才繼續（2026-09-24 第二次會議確認：標準不放寬）：
- 找到 1 位合格隊友（正式報名、JAPAC 在職、滿 21 歲、取得雇主同意；掛名 advisor 不算）
- GCP 已開通，預算警示已設好
- 完成至少 2 場訪談
- 分鏡稿交付

**任何一項沒做到**：隊友或 GCP 沒搞定 → 退出比賽，把訪談成果留作商業驗證；只有訪談或分鏡落後 → 延到 10/3，但 W2 的工作要同步開始。

### W2：10/1（四）– 10/7（三）— 資料與查詢函式

| 項目 | 負責 | 完成定義 |
|---|---|---|
| 模擬資料就緒（示範用 DuckDB，映像內建；可換 BigQuery，9/24 決議不切換）（主線異常＋正常情境＋回歸集案例） | Eddie | 劇本時間軸和 PRD 第 8 節一致 |
| 5 個固定查詢函式可以單獨呼叫 | Eddie | 每個函式都有單元測試 |
| Cloud Run 單一服務上線（已提前到 GCP 開通當天；min-instances=0，見「Cloud Run 常駐政策」） | Eddie | 公開網址可以開啟；資料用示範用 DuckDB（可換 BigQuery，不切換） |
| 10 題回歸集和測試計畫 | Quinn | `docs/qa/test-plan.md`，含 3 題干擾項和灰卡雙向案例 |
| 信心標籤規則 | Paula、Quinn | PRD Q4 結案 |
| Pitch 三句定稿（填入訪談的真實數字） | Sandy | 沒問到的數字標「待驗證」 |

### 10/8（四）里程碑

模擬資料、固定查詢函式、10 題回歸集全部就緒。

### W3：10/8（四）– 10/14（三）— Agent 與三畫面串通

| 項目 | 負責 | 完成定義 |
|---|---|---|
| Gemini function calling 串上 5 個函式（F3） | Eddie | 主線劇本可以跑出正確根因 |
| 三畫面（F1、F2、F4、F5、F6）＋工單與 QR（F7）＋情境切換（F8） | Eddie（實作）／Dana（視覺驗收） | 符合分鏡 |
| 速率限制、逾時退回快取 | Eddie | Quinn 驗過 |
| 第一次跑回歸集 | Quinn | 產出分數與失敗清單 |

### 10/10（六）停損點 ②：Agent 串通檢查

檢查標準：主線劇本從按下 Investigate 到出結論，Gemini 即時呼叫固定函式，結果正確。

**沒串通時的退路（採 Quinn 底線）**：
- **固定**：資料集只保留主線異常＋正常兩個情境，UI 縮成最小可 demo 的版本（例如產線圖只畫 Line 2）。證據卡回查原始列（F4）和灰卡（F6）是信任賣點，不能砍。
- **不能固定**：資料查詢（示範用 DuckDB，可換 BigQuery）和 Gemini 推論必須即時執行，**不退回全寫死劇本**。
- **理由**：官網要求「部署在 Google Cloud 的可運作原型」，全寫死可能被判定不符規定，導致整份作品失格，這個風險比 demo 不夠流暢大得多；而且我們的核心賣點是「每個結論都查得到證據」，寫死就等於自打嘴巴。Eddie 擔心的穩定性問題，改用「逐步驟快取＋標示 cached＋備援錄影」處理。
- 如果 10/10 連縮小範圍的即時版本都跑不動 → 老闆當天決定是否退出，不要拖到 10/16。

### 10/13（二）里程碑

Agent 和三畫面全部串通，主線與灰卡都能在 Cloud Run 上完整跑完。

### W4：10/15（四）– 10/18（日）— 部署、驗證、繳交

| 日期 | 項目 | 負責 |
|---|---|---|
| 10/15 | 過一遍上線前檢查清單：回歸集 ≥ 9/10、injection 與缺值測試、冷啟動實測、速率限制 | Quinn |
| 10/16 | 部署已於 9/24 完成，不再動；只做錄影前最後檢查 | Eddie／Quinn |
| 10/17 | min-instances 改成 1、暖機後錄 demo 加灰卡的備援影片；繳交後改回 0 | 老闆／Dana（錄影腳本）／Quinn |
| 10/16 | 繳交文案和 pitch 最終版 | Sandy、Paula |
| 10/17 | **繳交** | 老闆 |
| 10/18 | 緩衝日（官方截止日），不排任何新工作 | — |

## 預算觸頂停損點 ③（任何時間點）

- 觸發條件：GCP 花費達到老闆拍板上限的 90%。
- 處理：調低速率限制、盡量改用快取；開發期改用本機測試加上小量雲端驗證。對外 demo 以錄影為主，但 **Cloud Run 網址要保持可運作**（繳交需要）。
- 達到 100%：停止所有非必要的雲端呼叫，由老闆決定是否加碼；不能默默讓它繼續燒。

## 老闆本人必做事項

本週逐日清單（9/25 邀約＋徵隊友、9/26 預算＋GCP＋警示、9/27 真 Gemini、9/28 前約好 2 場訪談、10/1 停損）見最上面的「本週重點」。

| 事項 | 期限 | 備註 |
|---|---|---|
| 找到合格隊友並確認資格 | 10/1 | 正式報名、JAPAC 在職、21 歲以上、取得雇主同意 |
| 寄信向主辦確認隊友資格和「可運作原型」的定義 | 9/30 | 用 Quinn 擬好的稿 |
| 開通 GCP、設預算警示、拍板現金上限 | 9/26 | 上限參考 Felix 的提案；警示含「預測達 100%」；本機用 ADC，不下載服務帳號金鑰 |
| 完成 2–3 場廠長訪談 | 9/30 | 用 Sandy 的題綱；數字交給 Felix |
| 完成報名（含隊友） | 10/1 前確認報名方式與期限 | 報名期限如果更早，以官網為準 |
| 每週投入約 25 小時開發 | 每週 | 開發進度落後超過 1 天要立刻告訴 Paula，調整範圍 |
| 回報隊友、訪談、預算狀態；查試用抵免額到期日 | 10/1 前 | 第三次會議追加 |
| GCP 端設 Vertex AI API 每日配額上限 | 10/1 前 | 硬上限，數值待估算；預算警示只通知不擋 |
| min-instances 調 1 錄影，繳交後調回 0 | 10/17 | 見「部署後定案」 |
| 10/17 繳交 | 10/17 | 留 10/18 當緩衝 |
