# 會議紀錄：UI v2 之後的下一步與優化

- 日期：2026-09-24（第二次會議）
- 輪數：2 輪
- 與會：Paula（PM）、Eddie（工程師）、Dana（設計師）、Sandy（業務）、Felix（財務）、Quinn（QA）
- 會前狀態：UI v2 完成並推上 GitHub（commit `a395f57`）；pytest 196 passed／1 skipped／4 xfailed（BUG-009）。主持人以 Playwright + Edge 實測 1920×1080、1366×768、1280×720、758px：主流程都能走完，1280／1366 整頁不捲動；A1（結論出現後證據列只看得到 2–3 列）、A9（全景狀態字 15.8px）未達標；工單 QR 不顯示的 bug 已修好並補測試。尚未：真 Gemini、BigQuery、Cloud Run、訪談、GCP、隊友。
- 老闆交代：會後屬於程式端的修改直接執行，所有修改 push。

## 一句話結論

包裝已經超前，參賽資格與「AI 真的準」卻還是零證據。這週老闆衝隊友、GCP 和訪談，工程端在 GCP 開通前把 BUG-009、token 紀錄、假 client 測試、A1 版面和重試上限做完，開通當天半天內打通真 Gemini 和 Cloud Run。

## 共識

- **優先順序**：①老闆本人的資格項（隊友、GCP、訪談）→ ②真 Gemini＋最簡 Cloud Run（從 W2/W3 提前）→ ③BUG-009 → ④A1。
- **程式端立即執行（Eddie，約兩天，9/26 前）**：
  - BUG-009：NULL 感測值當成資料缺口，失敗的步驟不能一直轉圈；4 條 strict xfail 轉為 passed。
  - ENH-002：每次 Gemini 呼叫都記錄 input／output／thinking token 與輪數。
  - Gemini 與 BigQuery 的假 client 測試（多輪呼叫、強制 submit、逾時、參數型別），斷網也要全過。
  - A1＋小圖：精簡版門檻 959.98→1199.98（app.css 兩處一起改）、精簡版根因字 40→28px；寬度 ≥1600 時小圖 120×32→160×44。A1、小圖由 Eddie 實作，Dana 看截圖驗收。
  - A9：兩份 SVG 狀態字 26→27（最後做）。
  - Quinn 補充的遺漏風險：Gemini 逾時與 429 的重試次數要設上限。
- **A1 驗收標準（Dana）**：720 和 1080 高各拍空白、分析中、結論三態；720 高至少看到 3 列、1080 高 5 列不捲動；根因字 28px；1080 小圖 160×44 且不糊。
- **GCP 開通當天路徑**：預算警示 → 開 API → 本機 `test_gemini_live.py` 打第一次真 Gemini → R01／N01 各 10 次 → Cloud Run 先用映像內建的 DuckDB 上線 → 隔天切 BigQuery → 全部回歸集 `--repeat 3` → 手機實機掃 QR。
- **真 Gemini 通過標準（Quinn）**：
  - 第一天：R01 必須 10/10、N01 灰卡雙向全過，出現「錯根因＋High」直接 FAIL。
  - 全部回歸集：≥9/10、同一題 3 次結論一致。
  - 失敗時先分類再改 prompt，不放寬門檻；換成 2.5-flash 要整套重跑。
- **成本控制（Felix）**：開發期 Cloud Run min-instances=0，只在 10/16–10/18 與評審期常駐；R01／N01 那 10 次的 token 中位數 × 官方單價回填 `roi-model.csv` 第 15 列，10/1 依帳單回填第 16 列。
- **凍結**：PPT、影片、pitch 中的數字、S05／S06 對調、提前錄影，都等 10/10 停損點之後再議。
- **pitch 備案（Sandy）**：每個含數字的句子先準備無數字版；10/8 仍 0 場訪談時，改用無數字版並引用公開產業資料，註明「訪談進行中」，不編造客戶原話。

## 仍有分歧

- **本機憑證**：Eddie 要老闆在開通當天把專案 ID、帳單、服務帳號 JSON、BigQuery 權限與區域寫進 `.env`；README 與 deploy.md 寫的是本機用 `gcloud auth application-default login`、Cloud Run 用服務帳號，不需要下載金鑰檔。主持人建議照 README 用 ADC，**不要下載服務帳號 JSON 金鑰**；`.env` 只放專案 ID 與區域等非機密設定。
- 其他項目在第 2 輪都已收斂。

## 各角色待辦

| 角色 | 待辦 |
|---|---|
| Paula（PM） | 更新 `docs/prd.md`（動畫條款、UI v2 範圍）與 `docs/roadmap.md`（本週日期、凍結清單、開通當天路徑） |
| Eddie（工程師） | BUG-009、ENH-002 token 紀錄、假 client 測試、Gemini 逾時與 429 重試上限、A1＋小圖、A9（`app/`、`tests/`、`app/static/app.css`） |
| Dana（設計師） | 依截圖驗收 A1／小圖；寫驗收表；準備給 Sandy 邀約用的一張截圖 |
| Sandy（業務） | 協助老闆 9/25 起的邀約（`docs/sales/outreach-message.md` 短版）；Discord／GDG 徵隊友文；pitch 無數字版 |
| Felix（財務） | 9/27 回填 token 成本（`docs/finance/roi-model.csv` 第 15 列）；10/1 回填 GCP 月費（第 16 列） |
| Quinn（QA） | 程式端驗收清單 6 項；開通第一天的驗證門檻與冷啟動計時（`docs/qa/test-plan.md`） |

## 錢的問題

- 預算上限：**待估算**，請老闆 9/26 拍板。
- 每次調查的 Gemini 成本：**待估算**（9/27 量測）。
- GCP 月費：**待估算**（10/1 依帳單回填）。
- 常駐計費：開發期 min-instances=0 以避免 24 小時計費；常駐期間的成本**待估算**。

## 主要風險

1. 10/1 前沒有合格隊友或 GCP 沒開通 → 依停損點退出。
2. 196 條測試全是離線假資料，AI 準確度零證據。
3. preview 模型改名或下架（備 2.5-flash，換模型要整套重跑）。
4. Gemini 逾時／429 若沒有重試上限，會燒掉預算。
5. 本機沒有 Docker／gcloud，建置錯誤只能到 Cloud Build 看。
6. 「誰會付錢」沒有證據：pitch 仍有 33 處待驗證標記。

## 建議老闆下一步

1. **9/25**：照 `docs/sales/outreach-message.md` 發出第一批 10 則邀約，並在 GDG Taiwan／比賽 Discord 貼徵隊友文。
2. **9/26**：拍板預算上限、開通 GCP、設預算警示（50／90／100%＋預測達 100%）；本機用 `gcloud auth application-default login`，不要下載金鑰檔。
3. **9/27**：和 Eddie 一起打第一次真 Gemini（R01／N01 各 10 次）。

## 附錄：完整發言紀錄

### 第 1 輪

【Paula（PM）】我直說：我們的簡報、影片這些包裝做得很前面，但參賽資格那幾項幾乎是零。roadmap 列的 10/1 停損點有四項：隊友、GCP、2 場訪談、分鏡，實際只有分鏡算完成。到 10/8，我的排序是：1. 老闆本人 10/1 前搞定隊友、開通 GCP，訪談至少 2 場，這關沒過其他都白做。2. 真的呼叫 Gemini 一次，再做最簡單的 Cloud Run 部署，從 W2/W3 提前做，「可運作原型」沒做到，整份作品可能不符資格。3. 修 BUG-009，感測值缺值就卡在 Querying…，等於直接打臉灰卡主打的可信度。4. A1 證據列至少要看到 3 列，只調版面、不重做。要砍的：A9 字級、S05/S06 對調、提前錄影，都等 10/10 停損點過了再說。PPT 和影片先停著不改，PRD 的動畫條款我會後更新。

【Eddie（工程師）】同意 Paula 的排序，但②要等 GCP 開通。開通前我先做這幾件，合計約兩天：BUG-009（4 個 xfail 測試已經寫好，半天）；A1 只改 app.css，讓證據列露出 3 列；gemini_agent.py 用假 client 補測試，驗多輪呼叫、強制 submit、逾時；BigQuery 同樣用假 client 驗參數型別，SQL 都是純 ANSI，開通後再用 dry_run 驗。開通當天的最短路徑：先設預算警示，再開 API，本機跑 test_gemini_live.py 打第一次真 Gemini，Cloud Run 先用映像檔內建的 DuckDB 上線，BigQuery 隔天再切，這樣半天就有網址。風險有三個：preview 模型可能改名，要備好 2.5-flash；本機沒裝 Docker 和 gcloud，建置錯誤只能到 Cloud Build 看；min-instances=1 會一直計費，評審前再調上去。

【Dana（設計師）】我同意 Paula 把 A1 排進來，也同意 Eddie 只改 app.css。不過標準要分開訂：1080 看得到 5 列，720 至少 3 列，因為推理鏈就是我們的賣點。改兩處就夠：①精簡版門檻從 959.98 放寬到 1199.98，1080 也收合處置清單，結論卡不再是 536px 那麼高；②精簡版根因字從 40 降到 28px，不會再折成兩行。小圖不動列高，只在寬度 ≥1600 時把 120×32 放大到 160×44，看細節就點開大圖。A9 只差 0.2px，評審看不出來，但改法只是兩份 SVG 的狀態字 26→27，五分鐘就能結案。優先順序：A1 先，小圖其次，A9 最後。

【Sandy（業務）】同意 Paula 把訪談排第一。從評審角度看，最弱的不是 A1，是「誰會付錢」完全沒有證據。docs\pitch 四份素材裡還有 33 處待驗證標記，評審只要問一句「你問過幾位廠長？」就答不出來。這週這樣衝：outreach-message.md 明早 9/25 就發，LinkedIn 搜「廠長／生管課長」，加上台商協會，每天發 10 則，3 天沒回就發跟進版，目標 9/28 前約到 2 場。找隊友同時進行：到 GDG Taiwan 和比賽的 Discord 徵人，話術是「10/18 前每週 3 小時，負責訪談紀錄或 GCP，共同掛名」。pitch 素材先凍結，每句有數字的都先寫好一個「無數字版」，10/8 還拿不到數字，就改成訪談原話或整句刪掉，不要硬塞數字。

【Felix（財務）】同意 Eddie 開通後先設警示，我補三點。①上限金額待估算，老闆 9/26 拍板；警示 50/90/100% 看實際花費，再加一條「預測達 100%」。②min-instances=1 加 --no-cpu-throttling 是 24 小時計費，開發期一律 min=0，只在 10/16–10/18 和評審期常駐。③ENH-002 還沒做，gemini_agent.py 不記 token，請排進開通前那兩天，不然第一次真呼叫量不到東西。R01 跑 10 次，記每輪 input/output/thinking token 和輪數，乘單價填進 roi-model.csv 第 15 列，再乘 GLOBAL_RATE_LIMIT_PER_HOUR=60 算出每小時最多會花多少。停損不放寬：10/1 隊友或 GCP 沒到位就退出。

【Quinn（QA）】同意 Eddie 的順序，但開通設完預算警示後，先只跑 R01、N01，順便確認 log 有 token 數，過了才跑全集 --repeat 3，再上 Cloud Run，最後拿手機掃 QR。通過標準是：≥9/10、灰卡雙向全過、同一題 3 次結論一致，另外 R01 必須 3/3，出現「錯根因＋High」直接判 FAIL。沒過就先分出是哪種錯再改 prompt，不放寬門檻；如果換成 2.5-flash，要整套重跑。驗收方式：BUG-009 的四條 xfail 要轉成 passed；A1、小圖、A9 在 720／1080 截圖數列數、量字級；假 client 斷網也要能過。最大風險是那 196 條全用離線假資料跑，AI 到底準不準，現在還沒有任何證據。

### 第 2 輪

【Paula（PM）】定案如下。程式端馬上做：Eddie 修 BUG-009，四條 xfail 要轉成 passed；補 ENH-002 token 紀錄；Gemini／BigQuery 用假 client 測試，斷網也要全過。Dana 做 A1 和小圖，截圖要看到 720 至少 3 列、1080 有 5 列。A9 放最後。我負責更新 PRD 和 roadmap。老闆這週：9/25 發訪談邀約、找隊友；9/26 拍板預算、開 GCP、設預算警示；9/27 真 Gemini 跑 R01、N01；9/28 前約好 2 場訪談；10/1 停損標準不放寬。凍結 PPT、影片、pitch 數字、S05/S06 對調、提前錄影。Cloud Run 到 10/16 前都維持 min-instances=0。

【Eddie（工程師）】同意 Paula 的定案。我的清單大約兩天，9/26 前可以全部交：BUG-009 半天（NULL 讀值要當成缺口、失敗的步驟不能一直轉圈，strict xfail 轉成 passed）、ENH-002 token 紀錄半天、假 client 斷網測試半天、A1 半天。A1 和小圖我來做比較好。Dana 已經把數字定好，app.css 第 94 和 399 行兩處的門檻都要一起改，改完我自己跑測試、交給主持人截圖，Dana 看截圖驗收就好。開通當天請老闆把專案 ID、帳單、服務帳號 JSON、BigQuery 資料集權限和區域這幾項直接寫進 .env，不要貼在聊天裡。

【Dana（設計師）】同意 Eddie 做，app.css 第 94、399 行門檻一起改。720、1080 兩種尺寸各拍空白、分析中、結論三種狀態的截圖：720 至少 3 列、1080 顯示 5 列不捲動，根因字 28px，1080 的小圖 160×44 且不糊，A9 的 27px 最後補。這兩週我寫 design-spec 驗收表，也給 Sandy 準備邀約用的截圖。

【Sandy（業務）】同意 Paula 的定案，我補三件事。第一，老闆 9/25 起每天發 10 則邀約，用 LinkedIn 跟台商協會，照 docs/sales/outreach-message.md 的短版發，同一天在 GDG Taiwan／Discord 貼徵隊友文。第二，Dana 的 A1 截圖只放在對方接受後傳的完整版裡，一張就好，讓廠長一眼看懂「停線原因加證據」長什麼樣子。第三，如果到 10/8 還是 0 場訪談，pitch 就改用無數字版，把痛點改成引用公開的產業資料，並清楚寫出「訪談進行中」，不要編客戶的話。

【Felix（財務）】接 Paula 的分工，量測這樣定：9/27 由 Eddie 用 ENH-002 的 token 紀錄跑 R01、N01 各 10 次，我取中位數乘官方單價，回填 roi-model.csv 第 15 列。10/1 我照帳單把 GCP 月費回填第 16 列。9/26 請老闆拍板三件事：預算上限（待估算）、警示 50/90/100% 加預測、停損線不放寬。

【Quinn（QA）】程式端驗收清單：☐ BUG-009 那 4 個 xfail 拿掉後轉成 passed；☐ 斷網狀態下 pytest 全過；☐ ENH-002 每次呼叫都記下 token；☐ A1 截圖照 Dana 的標準；☐ app.css 的門檻回歸不能壞；☐ 196 條測試維持全過。開通第一天：R01/N01 各 10 次，Felix 就用這 10 次算 token，不另外多跑。R01 要 10/10 對，N01 灰卡雙向都要過，出現錯根因加 High 直接 FAIL。漏掉的風險有兩個：沒人負責 Gemini 逾時和 429 的重試上限，會把 10/1 停損燒穿；min=0 冷啟動第一次也要掐秒數。
