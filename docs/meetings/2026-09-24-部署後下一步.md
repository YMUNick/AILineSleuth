# 會議紀錄：部署與手機測試通過後的下一步

- 日期：2026-09-24（第三次會議）
- 輪數：2 輪
- 與會：Paula（PM）、Eddie（工程師）、Dana（設計師）、Sandy（業務）、Felix（財務）、Quinn（QA）
- 會前狀態：Cloud Run 已部署（`asia-southeast1`，`gemini-2.5-flash`，資料用映像內建 DuckDB，min 0／max 1）；真 Gemini 完整回歸集 GATE PASS（10/10 根因、2/2 證據不足、3 次一致、中位數 12.6 秒、0 重試）；老闆已用手機實測 QR 開工單 OK；PPT 已換成雲端真截圖；30 秒分鏡生圖 prompt 已備。隊友、訪談、預算拍板的狀態老闆尚未回報（10/1 停損點）。

## 一句話結論

部署、回歸、手機 QR 宣告完成不再動；BigQuery 砍掉、改寫成「DuckDB，可換 BigQuery」。10/10 前工程補 A1、每日上限，並把「90 秒」換成實測的「約 12 秒」；老闆 10/1 前處理隊友、訪談、預算與 GCP 配額；10/8 Quinn 在雲端驗五項；10/17 常駐錄影。

## 共識

- **宣告完成，不再動**：Cloud Run 部署、真 Gemini 回歸、手機 QR 流程。
- **BigQuery 砍掉**：文件、PPT 第 7 頁、roadmap、deploy.md 改寫成「示範用 DuckDB，可換 BigQuery」，不能寫成「已支援」。Google Cloud 的使用深度靠 Cloud Run＋Vertex AI Gemini 支撐。
- **X-Forwarded-For 偽造檢查延到繳交之後**，前提是每日上限要先擋在前面。
- **3.8-flash 只當備案**，錄影當天實際切換一次確認可用。
- **程式端立即執行（Eddie，10/10 前，約 4 小時）**：
  - A1：720 高的第一屏至少看到 3 列證據（約 2 小時）。
  - 每日調查上限 200 次：程式內全服務每日計數，超過時顯示友善畫面（約 1.5 小時）。200 是 Felix 的提議值，不是算出來的。
  - `docs/engineering/architecture.md`、`docs/roadmap.md`、`docs/engineering/deploy.md`、PPT 第 7 頁改寫 BigQuery 描述（約半小時）。
- **文案（Sandy）**：「90 秒」改成「實測約 12 秒找到根因」並附出處 `docs/qa/runs/README.md`；「40 分鐘」維持「待訪談驗證」。10/1 仍 0 場訪談時，pitch 拿掉 40 分鐘、只講 12 秒。主持人補充：「90 秒」出現在 README、`docs/pitch/one-pager.md`／`.html`／`.pdf`、PPT、pitch 逐字稿等多處，要一起改。
- **30 秒影片（Dana）**：01 夜班工廠用生成圖並標「示意」；05 用真手機實拍掃 QR；其餘 5 格用真錄屏（雲端真 Gemini 畫面）；片尾停在 PPT 封面。`docs/pitch/storyboard-30s-image-prompts.md` 改版只保留 01 的 prompt，10/3 前交。
- **雲端驗證五項（Quinn，10/8）**，通過標準：
  1. 完整回歸集全過。
  2. min-instances=1 時冷啟動低於 3 秒。
  3. 真 Gemini 下的 prompt injection 沒有造成外洩。
  4. 超過每日上限時顯示友善畫面。
  5. A1 在 720 高不破版。
- **min-instances**：10/17 錄影前調成 1，繳交後調回 0。max-instances 維持 1（每個實例各算各的上限）。
- **錄影當天（Quinn）**：確認 min=1、先打一次暖機、網路走手機熱點、3.8-flash 實際切換一次。

## 仍有分歧

- **每日上限的可靠度**：Eddie 主張在程式內計數，才有可驗證的超配額畫面；Felix 與主持人指出 min-instances=0 時實例閒置會關閉、計數會歸零，所以程式內的上限擋不太住。**結論是兩層都要**：程式內上限負責畫面與正常情況，老闆在 GCP 端另設 API 配額作為硬上限。GCP 端配額的具體數值**待估算**。

## 各角色待辦

| 角色 | 待辦 |
|---|---|
| Paula（PM） | 在 `docs/roadmap.md` 更新本次定案的日期（10/3、10/8、10/10、10/17） |
| Eddie（工程師） | A1（`app/static/app.css`）；每日上限 200 次（`app/main.py`、`app/config.py`、`.env.example`、測試）；BigQuery 描述改寫（`docs/engineering/architecture.md`、`docs/engineering/deploy.md`、`docs/roadmap.md`、`docs/pitch/deck/build-deck.js` 第 7 頁） |
| Dana（設計師） | 改版 `docs/pitch/storyboard-30s-image-prompts.md`，只留 01（10/3 前）；30 秒影片的錄屏分鏡 |
| Sandy（業務） | 「90 秒」→「實測約 12 秒」（README、`docs/pitch/one-pager.*`、PPT、`docs/pitch/pitch-script.md` 等）；附 demo 網址的升級版邀約訊息（`docs/sales/outreach-message.md`） |
| Felix（財務） | 週五前回填 `docs/finance/roi-model.csv` 第 15、16 列（實測 token × 官方單價，單價待估算） |
| Quinn（QA） | 10/8 雲端驗證五項並寫進 `docs/qa/runs/README.md` |

## 錢的問題

- 每次調查的 Gemini 費用：token 已實測（R01 輸入 5,686／輸出 284／thinking 1,749；N01 輸入 15,710／輸出 277／thinking 1,323），金額**待估算**（單價未回填）。
- 10/17 起 min-instances=1 常駐的費用：**待估算**（Eddie 用 GCP 計價工具算）。
- GCP 端每日配額的數值：**待估算**。
- 試用抵免額約 US$381，到期日**待老闆查**。

## 主要風險

1. 10/1 停損點：隊友、訪談、預算的狀態仍未知。
2. 「40 分鐘」沒有任何廠長背書，是評審眼中最弱的一環。
3. min-instances=0 時程式內計數會歸零，公開網址可能被刷，需要 GCP 端的硬上限。
4. 冷啟動是 demo 最大的風險，錄影與決賽前要常駐並暖機。
5. 砍 BigQuery 後，若文件或 PPT 還寫著「已支援」，評審會質疑。

## 建議老闆下一步

1. **10/1 前**：回報隊友、訪談、預算的狀態；到 Cloud Console 查試用抵免額的到期日。
2. **在 GCP 設 Vertex AI API 的每日配額上限**（IAM 與管理 → 配額），作為公開網址被刷時的硬上限。
3. **指派執行**：Eddie（A1、上限、文件）→ Sandy（12 秒文案、邀約）→ Dana（prompt 改版）→ Paula（roadmap）→ Felix（回填）；Quinn 等 Eddie 交件後 10/8 驗證。

## 附錄：完整發言紀錄

### 第 1 輪

【Paula（PM）】我宣告三件事完成：部署、回歸 GATE、手機 QR 流程，都不要再動。接下來的關鍵路徑只有一條：10/1 前先把隊友、訪談、預算的狀態拍板，接著照 storyboard-30s-image-prompts.md 把 30 秒影片做出來，10/10 前和 PPT 一起凍結。A1 在 720 高只看得到 2 列，這個要修，因為會出現在錄影畫面。BigQuery 砍掉。X-Forwarded-For 檢查延到繳交之後。3.8-flash 留著當備案就好，不做了。

【Eddie（工程師）】BigQuery 我同意 Paula 砍掉，但 build-deck.js 跟 architecture.md 都寫了 BigQuery，要改成「DuckDB，可換 BigQuery」，不然評審會問「示範的東西根本沒有」。Google Cloud 的使用深度就靠 Cloud Run 加 Gemini 來撐。X-Forwarded-For 可以延，但目前每個 IP 的限流能被偽造，繳交前至少要在 Gemini 設每日配額上限，防止被刷爆。A1 我用 CSS 的 min-height 加捲動，大約半天。min-instances 等 10/17 錄完影再調成 1，繳交後調回 0，冷啟動是 demo 最大的風險。

【Dana（設計師）】我同意 Eddie 用 min-height 加捲動，但驗收標準要寫清楚：720 高第一屏至少看到 3 列證據。不然錄影時評審根本看不到比對結果，可以把列高或摘要區壓一點。影片我建議七格裡五格用真錄屏，01 夜班工廠保留當開場，右下角加註「示意」；05 改成真手機實拍掃 QR，既然 QR 已經 OK，實拍比較有說服力。影片直接用 PPT 那組雲端截圖，顏色、字體一樣，片尾停在 PPT 封面。

【Sandy（業務）】同意 Paula 的關鍵路徑，但別忘了訪談本身才是整條路最卡的地方。邀約話術現在就升級，附上 demo 網址，改成「花 30 秒看它查一次停線，再告訴我你們現在要多久」，回覆率一定比只有文字高。README 裡的「90 秒」可以改寫成「實測約 12 秒出根因」，但要附上回歸的 elapsed_s 當證據。老實說，評審眼中最弱的還是「40 分鐘」這個數字，到現在還沒有一位廠長背書。

【Felix（財務）】Eddie 說得對，配額要設，但我先卡一個問題：docs/finance/roi-model.csv 第 15、16 列還是 0，第 28 列的毛利率 100% 不能信。我這週五前拿 README 實測的 token 數乘官方單價回填，thinking 按輸出價算，單價現在還是待估算。每日配額不要只看金額，建議先設 200 次／日（這是我提的上限，不是算出來的），再換算成台幣看划不划算。min-instances 常駐的月費待估算，請 Eddie 用 GCP 計價工具算一下。抵免額多久到期要看開通日，請老闆先查，順便設預算警示。

【Quinn（QA）】砍 BigQuery 我可以接受，前提是 Eddie 把文件寫成「可換」，不能寫成「已支援」。X-Forwarded-For 延後我也接受，但 Felix 提的每日 200 次上限要先設好擋在前面。繳交前一定要在雲端驗的五項：完整回歸、min 0 跟 min 1 的冷啟動秒數、真 Gemini 的 prompt injection（在 log 裡夾「忽略指示」）、超過配額時畫面顯示什麼、A1 在 720 螢幕上的顯示。錄影當天先確認 min-instances 是 1，先打一次暖機，網路走手機熱點，3.8-flash 備案也要當天實際切換一次。

### 第 2 輪

【Paula（PM）】定案如下。①程式端：Eddie 修 A1（720 第一屏至少 3 列），設每日上限 200 次，architecture.md 改寫成「DuckDB，可換 BigQuery」；Quinn 在雲端驗完五項，全部 PASS；Felix 回填 roi 第 15、16 列；Sandy 文案改成「實測約 12 秒」。②老闆：10/1 前拍板隊友、訪談、預算，並查抵免額到期日；10/10 影片和 PPT 凍結；10/17 設 min=1 錄影；繳交後調回 0。③部署和 QR 不再動；BigQuery 砍掉；XFF 延後；3.8-flash 只當備案。

【Eddie（工程師）】同意 Paula 的定案。我的清單有三項：A1 在 720 高度第一屏至少 3 列，約 2 小時；每日上限 200 次約 1.5 小時；architecture.md 改寫半小時。上限要寫在程式裡，用 GLOBAL 每日計數。GCP 配額限制的是 Gemini 的呼叫次數，超過時丟出的是 429，擋不到「一次調查」這個單位，Quinn 也就沒有能驗的超配額畫面。PPT 要一起改，第 7 頁改成「DuckDB，可換 BigQuery」。roadmap.md 和 deploy.md 也還寫著「切換到 BigQuery」，我順手同步，10/10 前交。

【Dana（設計師）】同意 Paula。A1 驗收：720 高的第一屏至少 3 列。影片：01 用生成圖並標「示意」，05 用真手機實拍，其餘五格錄屏，片尾停在 PPT 封面。storyboard-30s-image-prompts.md 要改版，只留 01 的 prompt，我 10/3 前交。

【Sandy（業務）】我改 README 產品介紹和 docs\pitch\one-pager.html 兩處，把「90 秒」改成「實測約 12 秒（docs\qa\runs\README.md）」，「40 分鐘」加註「待訪談驗證」。附 demo 網址的邀約訊息明天交。到 10/1 還是 0 場訪談的話，pitch 就拿掉 40 分鐘，只講 12 秒。

【Felix（財務）】我週五前回填 roi 第 15、16 列，token 用實測值，單價待估算。同意 Eddie 維持 200 次，但 min 0 時計數會歸零，老闆要在 GCP 設 API 每日配額上限，預算警示只會通知、不會擋。

【Quinn（QA）】Eddie 交件後，我 10/8 在雲端跑五項：回歸全過、冷啟動 min1 低於 3 秒、注入沒有外洩、超配額有顯示友善畫面、A1@720 不破版。老闆要先設好 GCP 配額。另外有個漏掉的風險：開到多個執行個體時，每個各算各的上限，最多執行個體數要設成 1。
